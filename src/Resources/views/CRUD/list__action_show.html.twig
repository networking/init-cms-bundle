{% if admin.hasRoute('show') %}
    <a href="{{ admin.generateObjectUrl('show', object) }}" class="btn btn-light btn-sm button-in-row" title="{{ 'action_show'|trans({}, 'SonataAdminBundle') }}">
        <i class="fa fa-search-plus"></i>
    </a>
{% endif %}