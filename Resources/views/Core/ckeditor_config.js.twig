/**
 * @license Copyright (c) 2003-2013, CKSource - Frederico Knabben. All rights reserved.
 * For licensing, see LICENSE.html or http://ckeditor.com/license
 */

CKEDITOR.editorConfig = function( config ) {
	// The configuration options below are needed when running CKEditor from source files.
    config.language = '{{ app.request.locale }}'

	config.skin = 'moono';
    config.filebrowserUploadUrl= "{{ path('admin_networking_initcms_media_uploadTextBlockFile') }}";
    config.filebrowserBrowseUrl= "{{ path('admin_networking_initcms_media_uploadedTextBlockFile')}}";
    config.filebrowserImageUploadUrl= "{{ path('admin_networking_initcms_media_uploadTextBlockImage') }}";
    config.filebrowserImageBrowseUrl="{{ path('admin_networking_initcms_media_uploadedTextBlockImage')}}";
    config.height = "450px";
	// %REMOVE_END%

	// Define changes to default configuration here.
	// For the complete reference:
	// http://docs.ckeditor.com/#!/api/CKEDITOR.config

	// The toolbar groups arrangement, optimized for two toolbar rows.
	config.toolbarGroups = [
		{ name: 'clipboard',   groups: [ 'clipboard', 'undo' ] },
		{ name: 'editing',     groups: [ 'find', 'selection', 'spellchecker' ] },
		{ name: 'links' },
		{ name: 'insert' },
		{ name: 'forms' },
		{ name: 'tools' },
		{ name: 'document',	   groups: [ 'mode', 'document', 'doctools' ] },
		{ name: 'others' },
		'/',
		{ name: 'basicstyles', groups: [ 'basicstyles', 'cleanup' ] },
		{ name: 'paragraph',   groups: [ 'list', 'indent', 'blocks', 'align' ] },
		{ name: 'styles' , groups: ['test'] },
		{ name: 'colors' },
		{ name: 'about' }
	];
    {% if ckeditor_style is defined %}
    config.stylesSet = '{{ ckeditor_style }}';
    {% endif %}

	// Remove some buttons, provided by the standard plugins, which we don't
	// need to have in the Standard(s) toolbar.
	config.removeButtons = 'Underline,Subscript,Superscript';
};
