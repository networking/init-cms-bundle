{% block admin_style %}
    {% if app.user is defined %}
        {% if is_granted('ROLE_SONATA_ADMIN') %}
            {# Override this block to add your own files! #}
            {% stylesheets filter='less,?yui_css'
            '@NetworkingInitCmsBundle/Resources/public/less/admin-navbar.less' %}
            <link href="{{ asset_url }}" type="text/css" rel="stylesheet" media="screen"/>
            {% endstylesheets %}
        {% endif %}
    {% endif %}
{% endblock admin_style %}


{% block admin_navbar %}
    {% if app.user is defined %}
        {% if is_granted('ROLE_SONATA_ADMIN') %}
            <div class="navbar navbar-fixed-top navbar-initcms">
                <div class="navbar-inner">
                    <div class="container">
                        <div class="row">
                            <div class="span5">
                                {% if admin_pool is defined %}
                                    <a href="{{ url('sonata_admin_dashboard') }}" class="brand">
                                        <span class="info-very-soft">initcms - </span>{{ admin_pool.title }}
                                    </a>
                                {% endif %}
                            </div>
                            <div class="span2 align-center">
                                {% if admin_pool is defined %}
                                    {% include admin_pool.getTemplate('user_block') %}
                                {% endif %}
                            </div>
                            <div class="span5">
                                {{ mopa_bootstrap_navbar('viewStatusMenuBlock') }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {% endif %}
    {% endif %}
{% endblock %}